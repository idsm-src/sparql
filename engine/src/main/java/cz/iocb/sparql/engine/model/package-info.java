/**
 * Abstract syntax tree of a SPARQL query: the query forms (SELECT, CONSTRUCT, ASK, DESCRIBE), the prologue with
 * prefixes, the dataset clause, projection and solution modifiers. Nodes of the tree carry the source range they were
 * parsed from.
 */
package cz.iocb.sparql.engine.model;
