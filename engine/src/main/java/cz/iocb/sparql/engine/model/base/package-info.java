/**
 * Common base of the AST: the element interface, source ranges, and the marker of complex elements that exist only
 * while the parse tree is converted.
 */
package cz.iocb.sparql.engine.model.base;
