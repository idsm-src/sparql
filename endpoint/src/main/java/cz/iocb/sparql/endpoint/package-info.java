/**
 * Servlet implementing the SPARQL 1.1 protocol over the engine, with the bundled YASGUI user interface, and the JNDI
 * factory that instantiates the deployment's {@link cz.iocb.sparql.engine.config.SparqlDatabaseConfiguration}.
 */
package cz.iocb.sparql.endpoint;
